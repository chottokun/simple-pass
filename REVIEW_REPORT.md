# SimplePASS - 網羅的コードレビュー＆批判的解析レポート 🔐

本レポートでは、PowerShell + WPFで実装されたポータブル型ローカルパスワードマネージャー「**SimplePASS**」のコードベース（v2.0仕様）を対象に、セキュリティ、アーキテクチャ、パフォーマンス、堅牢性（エラーハンドリング）、およびコード品質（PSScriptAnalyzer結果を含む）の観点から徹底的に批判的解析を行い、あぶり出された問題点と具体的な改善案についてまとめます。

---

## 1. 総合評価
SimplePASSは、外部サーバーへのデータ送信を行わないローカル完結型パスワード管理アプリケーションとして、非常に高いセキュリティ意識を持って設計・開発されています。
PBKDF2-SHA256、AES-256-CBC、およびHMAC-SHA256を組み合わせた堅牢な暗号化スキーム、定数時間比較によるタイミング攻撃対策、厳格なプロトコルバリデーションなど、個人の学習プロジェクトの域を超えた実用的なセキュリティ対策が実装されています。また、i18n対応や自動ロック、クリップボード非同期消去など、ユーザー体験と実用性を両立させています。

しかし、プラットフォームやPowerShell特有の言語仕様、WPF GUIのスレッド安全、メモリ管理、エラーハンドリングの漏れなどの領域において、**「潜在的なデータ競合」「機微データの残存リスク」「環境依存による動作不整合」**といった改善すべき懸念事項が存在します。

---

## 2. セキュリティ分析（Vulnerabilities & Critical Risks）

### ① 機微データのメモリ上での露出・生存時間
- **現状の実装**: `Lock-VaultApp` 関数において、`$script:MasterPassword = $null` および `$script:VaultEntries = @()` に設定したのち、`[System.GC]::Collect()` を呼び出すことでメモリ解放を試みています。
- **批判的・分析的指摘**:
  1. **`.NET String` の不変性（Immutability）**: `System.String` オブジェクト（`$script:MasterPassword` や各エントリーのパスワード）はメモリ上で不変であり、参照を `$null` にしたとしても、ガベージコレクション（GC）が実際にメモリ領域をクリアまたは上書きするまでは、メモリのヒープ領域にプレーンテキストで生存し続けます。
  2. **`PasswordBox.Password` のプレーンテキスト露出**: `PbMasterPassword.Password` や `PbFormPassword.Password` にアクセスした瞬間、.NETはプレーンテキストの String オブジェクトをヒープ上に作成します。
  3. **WPF `DataGrid` へのバインド**: `$script:VaultEntries` にはすべてのパスワードがプレーンテキストのまま含まれており、これが DataGrid にバインドされています。表示を伏せていたとしても、プロパティ自体はメモリ上にオブジェクトとして保持され続けます。
- **改善案**:
  - メモリダンプ対策として、機微情報を保持する部分（マスターパスワードやエントリーパスワード）に `.NET` の `System.Security.SecureString` クラスを活用する。または、バイト配列（`[byte[]]`）として格納し、使用後に `[Array]::Clear()` でゼロクリア（ゼロフィル）を明示的に実行する。

### ② クリップボード自動消去の実装におけるデータ競合（Race Condition）
- **現状の実装**: `Set-ClipboardWithAutoClear` 関数は、別スレッド（STA Runspace）を立ち上げ、30秒スリープした後に `[System.Windows.Clipboard]::GetText() -eq $copiedText` であればクリアする設計です。
- **批判的・分析的指摘**:
  - もしユーザーがSimplePASSからパスワードをコピーした5秒後に、別のアプリケーション（またはSimplePASSの別エントリー）で新しい別のテキストをクリップボードにコピーした場合、30秒のタイマーが切れた時点で「現在のクリップボード内容が、自分が30秒前にコピーしたものと完全に一致するか」を検証しています。
  - しかし、クリップボードの監視（Clipboard Viewer Chain）を行っていないため、複数の非同期タイマーが並走した場合、スレッド間でのクリアロジックが競合し、意図しないタイミングでクリップボードが消去される、または消去されずに残る不整合が発生する可能性があります。
- **改善案**:
  - クリップボード消去用のセッション/スレッドの管理状態をグローバルまたはシングルトンで保持し、新たにコピーが実行された場合は先行するクリア用バックグラウンドジョブをキャンセル（またはタイマーを延長・破棄）するコーディネーション機構を導入する。

### ③ 暗号化エラー時の詳細情報の漏洩（サイドチャネル/オラクル攻撃）
- **現状の実装**: HMAC検証の失敗時や、AES復号時のパディングエラーにおいて、詳細なエラーメッセージが `Write-AppLog` を通じてログに記録、または例外メッセージとして画面（`$txtLoginError`）にそのまま出力される可能性があります。
- **批判的・分析的指摘**:
  - 暗号文の破損とキーの不一致を明確に区別してエラーとしてフロントエンドに返してしまうと、攻撃者に暗号オラクル（Padding Oracle等）としての手がかりを与える可能性があります。
- **改善案**:
  - 暗号処理におけるエラーハンドリングは、常に一般的な「パスワードが正しくないか、データが破損しています」という統合エラーメッセージのみを返却し、内部のログにのみデバッグ詳細を出力（ただし機微データは含めない）するように統一する。

---

## 3. アーキテクチャ＆パフォーマンス分析（Architecture & Bottlenecks）

### ① 同期型I/OとUIスレッドのブロッキング
- **現状の実装**: 保管庫データの保存 (`Save-Vault`) や読み込み (`Load-Vault`)、CSV出力 (`Export-VaultToCsv`)、さらにはPBKDF2のストレッチング（100,000回）がすべて**WPF UIスレッドと同一スレッド上（同期）**で実行されています。
- **批判的・分析的指摘**:
  - PBKDF2の100,000回ストレッチング処理は、CPU集約型の重い処理です。また、ディスクへのI/O処理（特に保管庫ファイルが肥大化した場合やCSVエクスポート時）も時間がかかります。
  - これらが同期で実行されるため、ボタンをクリックした瞬間にUIが「応答なし（フリーズ）」状態になり、特に低スペック環境や暗号パラメータの引き上げ時に、極めてストレスフルなユーザー体験（UX）を与えます。
- **改善案**:
  - 暗号導出処理およびディスクI/O処理を、非同期タスク（`[System.Threading.Tasks.Task]::Run()` や PowerShellの非同期Runspace）にオフロードし、処理中はUIにローディングアニメーション（インジケーター）を表示する。

### ② 自動バックアップファイルのローテーション限界
- **現状の実装**: `Save-Vault` では、既存の `vault.json` を上書きする直前に `vault.json.bak` という固定名でバックアップを作成します。
- **批判的・分析的指摘**:
  - バックアップが常に1世代しか保持されないため、破損した保管庫データで連続して保存が実行された場合、正常だったバックアップファイルも破損データで即座に上書きされてしまいます（世代管理の欠落）。
- **改善案**:
  - タイムスタンプを付与した複数世代のバックアップ（例: `vault_20261025.json.bak`）を保持し、一定件数または日数を経過した古いバックアップを自動クリーンアップするローテーション方式へ移行する。

---

## 4. コード品質とPSScriptAnalyzer違反（Linter & Code Quality Issues）

PSScriptAnalyzer を用いた静的コード解析を実行した結果、以下の主要な違反が「あぶり出され」ました。これらは将来的なバグや可読性の低下を招くリスク要因です。

### ① 自動変数（Automatic Variables）への不適切な割り当て
- **警告内容**: `PSAvoidAssignmentToAutomaticVariable`
- **対象箇所**: `SimplePASS.ps1` 内の `$sender` に対する代入（EventHandlerのパラメータ）
  ```powershell
  $pbMasterPassword.Add_KeyDown({ param($sender, $e) ... })
  ```
- **詳細**: `$sender` はPowerShellの一部のスコープや特殊コンテキストで自動的に予約・変更される可能性があり、明示的にパラメータ名として再定義すると、予期せぬサイドエフェクトが生じる危険性があります。
- **改善案**: `$sender` の代わりに `$eventSender` や `$ctrl` といったカスタム変数名を使用する。

### ② 未使用パラメータおよび変数
- **警告内容**: `PSReviewUnusedParameter`, `PSUseDeclaredVarsMoreThanAssignment`
- **対象箇所**: イベントハンドラー（KeyDown, Clickなど）で宣言されているが、スクリプトブロック内で一度も参照されていない `$sender`。
- **改善案**: 不要なパラメータ定義は削除するか、`param($null, $e)` のように明記して可読性を高める。

### ③ 未承認動詞（Unapproved Verbs）の使用
- **警告内容**: `PSUseApprovedVerbs`
- **対象箇所**: `CryptoModule.psm1` 内の `Derive-KeyIVAndHmac` などのカスタム関数
- **詳細**: PowerShellの標準的な動詞（`Get`, `Set`, `New`, `Invoke` など）ではない動詞 `Derive` は、コマンドレットの一貫性を損ないます。
- **改善案**: `Get-DerivedKeyIVAndHmac` または `Initialize-KeyDerivation` のように、承認された動詞へリネームする。

### ④ 空のCatchブロック（Empty Catch Blocks）の存在
- **警告内容**: `PSAvoidUsingEmptyCatchBlock`
- **対象箇所**: `UtilsModule.psm1` および `LoggerModule.psm1` 内の例外キャッチ
- **詳細**: バックアップファイル作成やクリップボードクリア時の例外を `catch {}` で完全に握り潰しており、何が原因で失敗したのか（アクセス権不足、ファイルのロック等）が一切開発者にもユーザーにも伝わりません。
- **改善案**: 少なくとも `Write-AppLog -Level WARN` や `Write-Debug` を用い、例外内容をデバッグログに非表示で書き出すように改修する。

---

## 5. テスト網羅性と環境不整合（Test Completeness & Platform Compatibility）

### ① GUI / WPFテストの非Windows環境での全滅
- **現状の実装**: `RunAllTests.ps1` は全テスト（WPF GUI含む）を実行しますが、Linuxなどの非Windows環境（GitHub ActionsのLinuxコンテナ等）では `PresentationFramework` などのWindows Desktop関連アセンブリが見つからず、GUIテストがすべて致命的なエラー（FAIL）になります。
- **批判的・分析的指摘**:
  - シンプルに「テストが動かない」状態を許容することは、CI/CDパイプラインの一貫性を破壊します。また、ヘッドレスLinux環境向けにモックを作成している形跡はありますが、WPFクラス自体のロード処理で直接落ちているのが現状です。
- **改善案**:
  - プラットフォームを判定し、非Windows環境であればGUIテストを自動的に「スキップ（Skipped）」として分類し、終了ステータスを正常（0）にする条件分岐を追加する。

### ② CSVエクスポートデータのプレーンテキスト書き出し
- **現状の実装**: `Export-VaultToCsv` で、マスターパスワードで保護された保管庫からすべてのパスワードをプレーンテキストのCSVとしてディスクに直接エクスポートします。
- **批判的・分析的指摘**:
  - ユーザーが軽い気持ちで実行したCSV出力ファイルが、PCのダウンロードフォルダ等に永続的にプレーンテキストで残り続けることになり、これがウイルスや第三者によって容易に窃取される原因になります。
- **改善案**:
  - CSVエクスポートを実行する際、警告メッセージダイアログをより強調し、かつ可能であれば一時ファイルとしてのエクスポートや、自動消去、暗号化ZIP形式での出力オプションなどを検討する。

---

## 6. アプリケーション設計の限界（Design Limits & Improvements）

| 分野 | 現状の問題点 | 具体的な改善推奨アクション |
| :--- | :--- | :--- |
| **パスワード生成** | 記号文字の除外などのオプションがない（サービスによっては特定の記号が使えない場合がある）。 | 生成器にチェックボックス（英大・英小・数字・記号）をWPF上で動的に選択・変更できるGUIコントロールを実装する。 |
| **マスターパスワード** | パスワードの複雑性や強度のバリデーション（最小文字数や文字種の混在チェック）が一切存在しないため、極めて脆弱な（例: `123`）パスワードで保管庫が作成できてしまう。 | 新規作成およびパスワード変更時に、最小文字数（例: 8文字以上）およびエントロピー/強度インジケーターを表示し、強度が低い場合は警告する。 |
| **データベースフォーマット** | JSON形式を丸ごと暗号化しているため、一部のエントリーを検索・更新する際にも、全エントリーの復号およびメモリ展開が必要になる。 | 大規模なデータ（例: 数千件）を処理する場合を考慮し、エントリー単位での暗号化インデックス保持、または軽量DB（SQLite＋SQLCipherなど）の採用を中長期的に検討する。 |

---

## 7. 結論
SimplePASSはローカル完結型パスワードマネージャーとして非常に盤石な基本設計（暗号・整合性検証・URLホワイトリスト・クリップボード消去等）を有しています。
本レポートで明らかにした、**メモリ不変領域におけるStringの残存、クリップボード消去の競合、UI同期フリーズ、およびPSScriptAnalyzer指摘の自動変数/動詞の不適合**を改善することで、商用製品レベルに比肩する究極のローカル安全性を獲得できるポテンシャルを秘めています。
